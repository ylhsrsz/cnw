# Task2 UDP 抓包说明文档

**页眉：** `2026春计网课程实习抓包说明 | 创建者学号后四位：2308`

**页脚：** `创建者：2308 | 页码：第 __ 页 | 提交日期：____-__-__`

**创建者学号后四位：2308**

## 1. 文档信息

- 任务名称：Task2 UDP Socket Programming
- 文档名称：`udp_packet_capture_2308.md`
- 配套抓包文件：`resources/pcap/task2/task2_udp_capture_2308.pcapng`
- 配套日志目录：`resources/logs/task2/`
- 配套截图目录：`resources/images/task2/`

## 2. 实验目的

1. 观察 UDP 任务中应用层自定义协议的交互过程。
2. 验证“连接建立”阶段与“可靠传输”阶段的报文行为。
3. 观察累计确认、超时重传、发送窗口控制等机制。
4. 将 Wireshark 抓包结果与客户端、服务端日志进行对应分析。

## 3. 实验环境

- 操作系统：
- Python 版本：
- Wireshark 版本：
- 客户端 IP：
- 服务端 IP：
- 服务端端口：
- 输入文件名：
- 发送窗口大小：400 字节
- 分块范围：
- 超时时间：
- 学号后四位：2308

## 4. 抓包准备

### 4.1 过滤条件

建议填写实际使用的过滤器：

```text
udp.port == 9002
```

如使用了其他端口，请替换为实际端口。

### 4.2 启动命令记录

服务端命令：

```bash
python task2/udpserver.py --host 127.0.0.1 --port 9002 --output task2/udp_received_output.txt --log task2/run_log_server.txt --loss-rate 0.15
```

客户端命令：

```bash
python task2/udpclient.py --host 127.0.0.1 --port 9002 --input sample.txt --chunk-min 40 --chunk-max 80 --seed 42 --timeout-ms 300 --student-id-last4 2308 --log task2/run_log_client.txt
```

## 5. 操作流程

1. 启动 Wireshark，选择目标网卡并开始抓包。
2. 启动 UDP 服务端。
3. 启动 UDP 客户端。
4. 观察连接建立报文。
5. 观察数据发送、ACK、超时和重传报文。
6. 观察传输结束报文。
7. 停止抓包并保存到 `resources/pcap/task2/`。
8. 截取关键截图并保存到 `resources/images/task2/`。

## 6. 协议与关键报文观察点

### 6.1 连接建立阶段

建议观察以下报文：

- `CONNECT`
- `CONNECT_ACK`

请填写：

- 抓包截图：
- 报文时间戳：
- 是否能看到学号字段相关逻辑：
- 说明：

### 6.2 数据传输阶段

建议观察以下报文：

- `DATA`
- `ACK`

请填写：

- 抓包截图：
- 报文编号：
- 字节范围：
- 说明：
  - 是否符合 `40..80` 字节分块要求；
  - 是否符合发送窗口 `400` 字节要求。

### 6.3 超时与重传阶段

请重点记录：

- 哪一个数据包发生超时；
- 客户端何时打印重传信息；
- 抓包中是否出现对应重传报文；
- 服务端是否给出累计确认。

填写内容：

- 重传截图：
- 超时时间：
- 对应日志：
- 分析结论：

### 6.4 结束阶段

建议观察以下报文：

- `FIN`
- `FIN_ACK`

填写内容：

- 抓包截图：
- 报文时间戳：
- 说明：

## 7. 日志与抓包互证

### 7.1 连接建立互证

- 抓包时间戳：
- 日志时间戳：
- 对应事件：
- 结论：

### 7.2 ACK 互证

- 抓包时间戳：
- 日志时间戳：
- 对应事件：
- 结论：

### 7.3 重传互证

- 抓包时间戳：
- 日志时间戳：
- 对应事件：
- 结论：

## 8. 可靠性机制分析

请从以下方面进行分析：

1. 连接建立阶段如何模拟“面向连接”；
2. 发送窗口为何固定为 400 字节；
3. 分块范围为何限定在 `40..80` 字节；
4. ACK 为何采用累计确认；
5. 发生丢包时为何会触发重传；
6. 抓包中如何判断某个数据包是重传包。

## 9. 数据一致性验证

- 服务端输出文件路径：`task2/udp_received_output.txt`
- 校验方式：
  - 与原始输入文件对比；
  - 检查服务端日志中的接收完成信息；
  - 检查 FIN / FIN_ACK 是否正常出现。

记录结果：

- 是否验证通过：
- 校验方法：
- 结论：

## 10. 问题与解决方案

建议填写：

1. 分块与窗口控制如何实现；
2. 为什么会出现重复 ACK；
3. 丢包模拟如何影响抓包现象；
4. 如何根据抓包定位超时重传问题。

## 11. 结论

请总结：

- 是否成功完成 UDP 可靠传输抓包；
- 是否观察到连接建立、数据发送、ACK、重传、结束阶段；
- 程序日志、抓包结果、输出文件是否三者一致；
- 是否满足课程提交要求。
